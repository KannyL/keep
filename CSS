.hero-content h1{
    font-size:4rem;
    color:var(--lunar-yellow);
    margin-top:20px;
    text-shadow:2px 2px 8px rgba(0,0,0,.2);
}
nav{
    background:rgba(112,25,29,0.95);
    backdrop-filter: blur(10px);
}
